# 智能体指令（精简版）

本文件是项目的统一基线规则；各智能体的独立规则见 `.opencode/agents/*.md`。

## 1) 统一基线（所有智能体）

1. 不破坏现有构建与测试。
2. 匹配仓库既有代码风格与结构。
3. 关键结论必须有证据（路径/命令/来源）。
4. 用户指令优先；不确定先澄清。
5. 禁止硬编码密钥、凭据和敏感信息。
6. 工作量按智能体实际速度评估，禁止用人天估算。
7. **外部参考代码只读**：<!-- 在此声明只读目录，例如 `src-ref/` -->用户声明的外部代码库严格只读——仅允许 Read/Grep/Glob，禁止编辑、写入、删除或 git 操作。后续新增目录由用户在此声明。
8. **版本先行**：涉及框架、平台、SDK、CLI 等外部依赖的开发或排障任务，动手前必须先确认当前实际运行版本（`--version` / `package.json` / lock 文件），再查阅**对应版本**的文档或源码。禁止直接引用 latest 文档作为当前版本依据。

## 2) 规则优先级

**用户指令 > AGENTS.md > .opencode/agents/\<role\>.md**

角色文件只做增量规则，不得覆盖本文件硬约束。回答"规则/职责/边界"类问题时，必须先读取 AGENTS.md 与对应角色文件再回答。

## 3) 角色与规则文件映射

所有角色文件位于 `.opencode/agents/` 目录。

| 角色 | 代号 | 文件 | 模型 | 说明 |
|------|------|------|------|------|
| 主智能体（主宰） | overmind | `overmind.md` | claude-opus-4.6 | primary，调度中枢 |
| 追风探子 | `@explorer` | `explorer.md` | gpt-5.4 | 代码与配置定位 |
| 藏经阁主 | `@librarian` | `librarian.md` | gpt-5.4 | API/文档考据 |
| 天机长老 | `@oracle` | `oracle.md` | gpt-5.4 | 高风险决策与权衡 |
| 妙手画师 | `@designer` | `designer.md` | gemini-3.1-pro-preview | 界面设计（多模态） |
| 神工匠徒 | `@fixer` | `fixer.md` | gpt-5.4 | 明确规格下实施改动 |
| 议会 | `@council` | `council.md` | claude-opus-4.6 | 高争议双模型裁决 |
| 议员 Alpha | `@councillor-alpha` | `councillor-alpha.md` | gpt-5.4 | hidden，只读分析 |
| 议员 Beta | `@councillor-beta` | `councillor-beta.md` | claude-opus-4.6 | hidden，只读分析 |

> **模型策略**：调度/裁决用强模型，执行/分析用高效模型，设计用多模态模型。双议员使用异构模型以保证独立视角。
> **议员约束**：hidden 且 edit/bash deny，仅基于证据包输出分析结论，不可修改文件或执行命令。

## 4) 协作与输出规范

1. 先结论，后证据；避免冗长过程复述。
2. 可并行任务并行执行；有依赖则串行。
3. 无验证不宣称"已完成/已修复"。
4. 禁止输出 think/推理过程，仅输出结果；think 和输出用中文。
5. **决策升级**：overmind 自判 → 内部咨询 `@oracle` → 才问用户（仅限偏好/不可逆/歧义/高成本/超范围 5 类）。高争议需双模型独立裁决时启用 `@council`。
6. **角色委派**：各角色"禁止做"的事项须委派至对应角色（详见各角色文件），不得长期越界硬做。

## 5) 长文写作约束

预估产出超 500 行或含 3 个以上章节时，必须先产出大纲经用户确认后再写正文。触发时加载 `outline-first-writing` skill。

## 6) 上下文沉淀规则

1. **及时固化**：浏览器探索、API 抓包等新发现，验证后必须在当次会话内写入 skill reference 文档，不得仅留在会话上下文。
2. **触发时机**：连续 3 条以上新发现，或会话即将结束时，主动检查并沉淀。
3. **沉淀目标**：skill reference（`.opencode/skills/*/reference/*.md`）优先；跨 skill 通用知识写入 `docs/` 对应目录。

## 7) 工具使用约定

1. **agent-browser 默认无头**：无需加 `--headless`；有头模式才加 `--headed`。所有命令统一带 `--json`。
2. **直接定位优先，snapshot 兜底**：优先用 `find`/`eval`/`wait` 定位元素。仅在直接定位失败、需查看动态数据或探索未知页面时用 `snap.sh`。截图仅限 snapshot 不可读内容、异常确认或用户要求。
3. **浏览器输出最小化**：原始 JSON（长 URL、snapshot 树、screenshot base64）禁止向用户转发，内部消化后只输出简短状态摘要。仅操作失败需用户协助时才输出错误上下文。

## 7a) 凭据与安全

1. **凭据零容忍**：禁止在任何文件中明文存储密钥、凭据和敏感信息。敏感信息统一存 `.env` 或 `accounts.yaml`，代码中只写引用路径。

## 7b) 工具韧性规则

1. **3 次重试上限**：同一调用连续失败 3 次后必须停止，切换备选方案并向用户报告。
2. **备选方案**：Write 失败 → Edit 分块写入；Bash 超时 → 拆分命令或加 timeout；Task 子代理失败 → 降级直接执行。
3. **分块策略**：大文件优先分块写入（先创建 + 逐段 Edit），而非一次性 Write。
4. **API 重试**：非幂等请求（POST/PUT）失败后先确认状态再决定；幂等请求（GET）可重试 3 次。

## 8) 项目既有硬性约定

1. 页面设计由 `@designer` 主责；原型修改需新建文件，不覆盖历史版本。
2. 每个 skill 的脚本必须放在该 skill 目录的 `scripts/` 子目录。
3. 调度子智能体时 prompt 必须包含对应角色规则文件路径。
4. `AGENTS.md` 或角色文件变更时，必须同步检查路径与优先级一致性。
5. 文档按类别存放 `docs/` 下（`guides/` / `specs/` / `plans/` / `decisions/` / `prototypes/` / `reports/` / `notes/`）。中文撰写、中文语义命名（可含日期前缀）。新会话读 `docs/文档导航.md`；目录变更后同步更新。`文档导航.md` 的"项目阶段总览"段落在文档状态变更时同步更新。
